// Background.tsx
const Background = () => {
  // Apply the primary color as the background
  return (
    <div className="fixed top-0 left-0 w-full h-full bg-primary z-[-1]"></div>
  );
};

export default Background;
